Añade aquí `icon.png` (1024×1024), `splash-icon.png` y `adaptive-icon.png` para builds de producción.
Expo puede generar placeholders con: `npx expo install expo-splash-screen` y assets por defecto del template.
